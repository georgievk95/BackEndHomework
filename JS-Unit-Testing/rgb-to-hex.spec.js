import { rgbToHexColor } from "./rgb-to-hex.js";
import { expect } from "chai";

describe('rgbToHexColor', () => {
    it('should return undefined if red value is out of parameter', () => {

    })

    it('should return undefined if green value is out of parameter', () => {

    })

    it('should return undefined if blue value is out of parameter', () => {

    })

    it('should return correct hex value if a correct rgb is given', () => {
        // Arrange
         
        // Act
        
        // Assert

    })
})